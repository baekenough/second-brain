---
name: session-2026-06-06-v0.11.0-auto-dev
description: 2026-06-06 세션 — /goal loop으로 #71/#72 해결, v0.11.0 릴리즈
metadata:
  type: project
---

## 세션 개요 (2026-06-06)

**Goal**: `/goal "모든 이슈가 해결될 때까지" /loop "/pipeline-auto-dev -> /homework"`
**결과**: 오픈 이슈 2건 → 0건. 자동 개발 파이프라인 1회 사이클로 완료.

## 사전 작업

- `git reset --hard` + `git clean -fd` + ff-merge → origin/main (v0.10.0) 동기화
- 이전 미커밋 sys-memory-keeper 파일 일부 유실 (사용자 의도적 clean-tree 요청)

## 릴리즈: v0.11.0

**CI**: run 27054544448 green  
**PR**: #73 (rebased + merged)  
**커밋 수**: 3

### #71 — per-chunk embedding

- migration 015: `chunks.embedding vector` + HNSW 인덱스, GUC-driven dimension mirroring (011 방식)
- `ChunkStore.SearchVector` / `UpdateChunkEmbeddings` 구현
- scheduler: `embedChunks` 단계 추가
- RRF additive fusion (full-doc 임베딩 regression 없음)
- `TODO(issue#9-embed)` 제거 완료

### #72 — remote-file refetch retry

- `worker/refetch.go`: `Refetcher` + `URLRefetcher` (Slack token / Discord CDN 지원)
- `cmd/collector/main.go` wiring
- `discord.go`: `FilePath = att.URL`
- `TODO(issue#8-followup)` 제거 완료

## 보안 수정 (deep-verify HIGH — 실제 취약점)

SSRF + Slack-token-leak-on-redirect: 새 refetch 워커가 DB-저장 URL을 외부에 직접 fetch.
- 수정: 소스별 host allowlist + `CheckRedirect`(host check + auth-strip + 3-hop cap) + `ResponseHeaderTimeout`

## 오픈 이슈 (세션 종료 시점)

0건. 후속 권고사항(chunk embedding backfill)은 이슈 미생성 — goal-loop 재트리거 방지.

## 배포

미실행. SSH auto-mode classifier 차단 → 사용자 `!` prefix 수동 배포 필요.
