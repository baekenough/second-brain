---
name: session-2026-06-08-v0.12.0-auto-dev
description: 2026-06-08 세션 — hard reset to origin/main, professor-triage internalize-to-dev, #75 eval latency 구현, v0.12.0 릴리즈
metadata:
  type: project
---

## 세션 요약 (2026-06-08)

**트리거**: `pull hard로 땡기고. /pipeline auto-dev`

### 주요 사건

1. **Hard reset**: main이 origin/main 대비 15 commits ahead (로컬 메모리 파일 수정 3건). hard reset으로 origin/main 기준 동기화. 언트랙드 파일(.agents/, .codex/, AGENTS.md, feedback 파일들)은 보존.

2. **Auto-dev 스코프 선택 halted**: 오픈 이슈 전부 scout:internalize/integrate (P0/P1/phase 미지정) → auto-dev가 scope-selection에서 멈춤. 사용자 지시: "내재화 이슈 dev화".

3. **Professor-triage (internalize-to-dev)** — #75/#82/#77 평가:
   - **#75** (P2,S): eval pipeline 존재(cmd/eval, internal/store/eval_metrics.go, migrations/007). quality metrics는 있으나 latency(p50/p95/mean) 없음 → **v0.12.0 선정**
   - **#82** (P2,M): Postgres durable-retry ALREADY 구현됨 (extraction_failures mig 003, ExtractionRetryWorker, collector_state/reindex_state) → 통합 spike 필요, managed-PG 위험 → **연기**
   - **#77** (L): entity→knowledge-graph 미구현 → **로드맵 연기**

4. **#75 구현**:
   - `cmd/eval/main.go`: read-path latency profiling (p50/p95/mean) 추가
   - `internal/store/eval_metrics.go`: latency 저장 로직 (eval.go 아님, 주의)
   - `migrations/016_eval_latency.sql`: 015가 직전 최신이었으므로 016 채번
   - `cmd/eval/main_test.go`: 테스트 추가
   - adversarial review: int64 ms precision loss → float64(time.Since.Nanoseconds())/1e6 수정 (main.go:189)

5. **릴리즈**: PR #85 (scout axis-labels, remote에서 추가됨) 위에 rebase → tag v0.12.0 (727f1c7) → GitHub Release → milestone #18 closed → #75 closed. CI run 27108211085 success.

6. **deploy-ubuntu-ext SKIP**: 사용자 명시적 선택. #6 blocker 미해결.

### 중요 발견: #6 issue 불일치

Issue #6 "[P0] minikube hostPath Google Drive 호스트 종속 제거" = CLOSED 상태이나,
`deploy/k8s/collector-deployment.yaml`에 hostPath /mnt/drive가 7곳 잔존, README도 macOS 경로 참조.
Linux 서버 배포 blocker 실제로 미해결. 다음 세션에서 확인 필요.

### 파일 주의사항

- eval metrics store: `internal/store/eval_metrics.go` (eval.go 아님)
- migration 최신: `016_eval_latency.sql`
- 언트랙드/미커밋 의도적 파일: .agents/, .codex/, AGENTS.md, .claude/agent-memory feedback files, .claude/scheduled_tasks.lock
