# Release Plan — v0.4.0 (Generated 2026-05-15)

> Source: scope-selection from auto-dev pipeline (2026-05-15)
> Triage source: professor-triage scoped to release manifest
> Previous tag: v0.3.1 → next: v0.4.0 (minor — new user-facing feature)

## v0.4.0 Release Plan

**Estimated scope**: M total | **Issues**: 2 | **Parallelizable**: 1 (close-only)

| # | Priority | Size | Title | Type |
|---|----------|------|-------|------|
| #47 | P2 | M | feat(setup): Collector 채널 대화형 wizard | implement |
| #46 | P2 | XS | docs(research): NL ABANDON | close-only (already wontfix) |

### Implementation Order

1. **#47** — Collector setup wizard with 7-channel catalog (be-go-backend-expert)
   - New package `internal/setup/` with `wizard.go`, `messages.go`, `envwriter.go`, `wizard_test.go`
   - Subcommand `setup` in `cmd/collector/main.go` via `os.Args[1]` dispatch
   - Build tag isolation `//go:build setup` to protect runtime image size
   - Library: `github.com/AlecAivazis/survey/v2` (smaller deps than huh)
   - 7 channels: filesystem, slack, github, gdrive, notion, telegram (collector-managed) + discord (server-managed, info-only menu entry)
   - `.env` backup as `.env.bak.<timestamp>` if exists, prompt for overwrite
   - Korean/English message catalog via `LANG` env var
   - Update `.env.example` to add missing telegram section (regression fix)
   - Update `README.md` and `README.en.md` 빠른 시작 with `go run -tags setup ./cmd/collector setup`
2. **#46** — Close with comment "Decision recorded; no implementation. Closed in v0.4.0." (mgr-gitnerd at release step)

### Scope Boundaries (out of scope for v0.4.0)

- Embedding source wizard (CliProxy vs OpenAI vs none) — defer to follow-up issue
- `kubectl create secret` k8s helper — defer
- Token format validators (xoxb-, ghp_, etc.) — nice-to-have, not blocker

### Risks / Notes

- Triage flagged ISSUE BODY/CODE asymmetry: body says 4 channels, code has 7. Wizard MUST handle all 7 to avoid silent skip regression for telegram/notion users.
- Build tag isolation prevents bloating runtime collector image. Setup binary is dev/onboarding only.
- TUI testing via `--non-interactive` flag with golden file fixtures.
- discord uses different daemon (server, not collector) — wizard should display info note, not generate keys for it.

### Files Touched (estimate)

| Path | Action |
|------|--------|
| internal/setup/wizard.go | new |
| internal/setup/messages.go | new |
| internal/setup/envwriter.go | new |
| internal/setup/wizard_test.go | new |
| internal/setup/testdata/*.env | new (golden) |
| cmd/collector/main.go | modify (subcommand dispatch) |
| .env.example | modify (add telegram section) |
| README.md | modify (빠른 시작) |
| README.en.md | modify (Quick start) |
| go.mod / go.sum | modify (survey/v2) |

## Summary

| Release | Issues | Size | P1 | P2 | P3 |
|---------|--------|------|----|----|-----|
| v0.4.0 | 2 | M | 0 | 2 | 0 |
