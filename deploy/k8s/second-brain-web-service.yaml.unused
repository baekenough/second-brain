apiVersion: v1
kind: Service
metadata:
  name: second-brain-web
  namespace: second-brain
  labels:
    app: second-brain-web
spec:
  type: NodePort
  selector:
    app: second-brain-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30300
